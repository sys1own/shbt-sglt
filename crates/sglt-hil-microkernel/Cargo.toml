[package]
name = "sglt-hil-microkernel"
version = "0.1.0"
edition.workspace = true
license.workspace = true
build = "build.rs"
description = "Bare-metal HIL bridge: AVX-512 shunt interlock, quench recovery, POSIX shm ring."

[dependencies]
libc = { workspace = true }

[build-dependencies]
cc = { workspace = true }
